type Knight inherits Person {
    name() => "Sir" @@ base();

    wacala(){
        self.say_my_name();
    }
}
type Person(first, last)  {
    firstname = first;
    lastname = last;

    say_my_name(){print(self.name());}
    name() => self.firstname @@ self.lastname;
    hash() : Number {
        5;
    }
}

{
    let leo=new Person("Edian","Broche \n Castro") in{
        leo.say_my_name();
    };
    
};
