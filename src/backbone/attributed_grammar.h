#ifndef ATTRIBUTEDGRAMMAR_H
#define ATTRIBUTEDGRAMMAR_H

#include "grammar.h"

template <typename T>
class attributed_grammar : public grammar {
public:
    attributed_grammar();
};

#endif // ATTRIBUTEDGRAMMAR_H
